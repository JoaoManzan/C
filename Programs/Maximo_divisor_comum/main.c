#include <stdio.h>
#include <stdlib.h>

int main()
{
    int M, N, MDC,  ;
    printf(" Entre com os numeros desejados:\n");
        scanf("%d", &M);
        scanf("%d", &N);

            if(N == 0 || M == 0){
                if (N > M){
                    printf("O MDC desse numeros e: %d", N);
                    return 0;
                else
                    printf("O MDC desse numeros e: %d", M);
            return 0;}}
        if (N > M){
            switch(N, M);
        }
        MDC =

        printf("O MDC desse numeros e: %d", MDC);
    return 0;
}
